# Same as Phase 0, plus the official Anthropic (Claude) library so our server
# can call Claude. Railway reads this file and installs each one automatically.
#
#   fastapi            - the web-server framework our code is built on
#   uvicorn            - the program that actually *runs* a FastAPI server
#   twilio             - Twilio's official library (gives us MessagingResponse)
#   python-multipart   - lets FastAPI read the form data Twilio sends us
#   anthropic          - the official Claude library (NEW in Phase 1)

fastapi==0.115.6
uvicorn[standard]==0.34.0
twilio==9.4.1
python-multipart==0.0.20
anthropic==0.69.0
