# Phase 3.5 libraries plus APScheduler for the background jobs (reminders,
# morning briefing, urgent-email poll). No new external services — APScheduler
# runs inside the FastAPI process.
#
#   ...as before...
#   apscheduler  - runs jobs on a clock (intervals + fixed times), in-process

fastapi==0.115.6
uvicorn[standard]==0.34.0
twilio==9.4.1
python-multipart==0.0.20
anthropic==0.69.0
google-api-python-client==2.156.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
apscheduler==3.11.0
